
DESTDIR=
PREFIX=C:
BINDIR=$(PREFIX)\bin
LIBDIR=$(PREFIX)\lib
INCLUDEDIR=$(PREFIX)\include
SYSCONFDIR=\etc

CC=gcc
CFLAGS=-march=native -mtune=native -O3

SUBDIRS=libdanh quadequi datatypes

all: bin out $(SUBDIRS)

.PHONY: $(SUBDIRS) test clean install uninstall

bin:
	mkdir bin

out:
	mkdir out



libdanh:
	$(CC) $(CFLAGS) -c libdanh/danh.c -o out/danh.o
	$(CC) $(CFLAGS) -c libdanh/math.c -o out/math.o
	$(CC) $(CFLAGS) -c libdanh/types.c -o out/types.o
	$(CC) -shared -Wl,-soname,libdanh.dll out/danh.o out/math.o out/types.o -o bin/libdanh.dll

test:
	$(CC) $(CFLAGS) test/sqrt.c -o bin/sqrt.exe -L./bin -ldanh
	$(CC) $(CFLAGS) test/convert.c -o bin/convert.exe -L./bin -ldanh

quadequi:
	$(CC) $(CFLAGS) quadequi/main.c -o bin/quadequi.exe -L./bin -ldanh

datatypes:
	$(CC) $(CFLAGS) datatypes/main.c -o bin/datatypes.exe

clean:
	del /Q out\*
	del /Q bin\*
	rmdir /Q out
	rmdir /Q bin


install:
	copy /Y bin\* $(DESTDIR)$(BINDIR)
	copy /Y libdanh.pc $(DESTDIR)$(LIBDIR)\pkgconfig\libdanh.pc
	copy /Y include\* $(DESTDIR)$(INCLUDEDIR)\libdanh


uninstall:
	del $(DESTDIR)$(BINDIR)\libdanh.dll
	del $(DESTDIR)$(LIBDIR)\pkgconfig\libdanh.pc
	del /Q $(DESTDIR)$(INCLUDEDIR)\libdanh
	del $(DESTDIR)$(BINDIR)\datatypes.exe
	del $(DESTDIR)$(BINDIR)\quadequi.exe

********************** DANHUTILS ********************

Version: 0.0.8
The oldest file: 2011-03-19 [52] datatypes/main.c

Danhutils is set of miscelaneous utilities, mostly using Libdanh library.

Libdanh is general purpose C library with various functions. Libdanh is
shipped with Danhutils.

Now Contains:

libdanh: 
	- version: 0.8 rev. 5
	C library with some functions, for example sqrt_double_dh() as implemetation of sqrt()
	from standard library math

quadequi:
	- version: 0.6
	Computes roots of equation "ax^2 + bx + c = 0" if a b c values given as command line arguments
	and prints x1 and x2 values.

datatypes:
	Prints sizeof() of standard types and sometimes max value.

	----------------------------------------------------------------
	
BUILDING:

GNU Linux:
	$ cd /home/user/src/danhutils
	$ make
	$ make install
	
Windows (MinGW32): Before running mingw32-make you have to install GCC and make from MinGW package from http://www.mingw.org/
	> cd C:\Users\user\src\danhutils
	> mingw32-make -f Makefile-mingw32
	> mingw32-make -f Makefile-mingw32 install

